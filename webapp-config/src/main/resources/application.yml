spring:
  application:
    name: jira-statistics-indicator

  profiles:
    include:
      - web-adapter
      - core
      - statistics-adapter
      - jira-adapter
      - spreadsheet-document-adapter
      - statistics-jpa
      - jira-api
      - microsoft-document

  devtools:
    add-properties: false
    restart:
      enabled: false